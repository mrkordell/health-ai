/**
 * Shared types and utilities for Health AI monorepo.
 *
 * RULES:
 * - Only export types, enums, and constants
 * - No business logic (belongs in api/services)
 * - No React components (belongs in web)
 */

// Placeholder - types will be added as needed in later tickets
export type PlaceholderType = Record<string, never>;
